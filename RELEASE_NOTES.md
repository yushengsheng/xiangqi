# 象棋盘面同步器发布说明

## v1.2.0 — Windows 微信小程序适配与实盘稳定性修复

- 新增微信「天天象棋」小程序横屏识别模板，并通过棋盘横竖线动态拟合 9×10 网格；窗口放大、缩小、宽窄留白变化均会自动重新定位。
- Windows 优先使用 Graphics Capture 读取目标窗口合成画面，避免其它窗口遮挡进入棋盘截图；保留 PrintWindow 与精确窗口区域回退。
- 修复清理缓存时把抬子、落子、吃子动画当成完整盘面的缺陷。旧盘面会一直保留，只有连续完整帧才能原子替换。
- 修复小程序右下木纹被识别为棋子占位、再被全盘约束错误补成红马的问题；低置信度木纹不再进入占位与棋子推断。
- 整盘重锚改为四帧及时间双门槛，单步合法走子仍保持快速双帧确认；将/帅、马及其它棋子的高亮光圈继续由占位信息安全恢复。
- 新增 Windows 隐藏启动器、本地 Pikafish Windows 通用版、自动端口避让和只读实盘看板；发布模式下 Pikafish 失败会明确报错，不再静默切换另一套棋力。
- 新增清缓存半识别帧、微信窗口多尺寸、木纹幽灵棋子、将帅高亮、马落点光圈、捕获异常和窗口重开等回归。44 项专项测试及完整发布测试套件全部通过。
- 系统要求：Windows 10/11（微信小程序、天天象棋客户端或常见模拟器）或原有 macOS 14+ 环境。游戏平台更换棋子皮肤或整体棋盘美术后仍需重新采集模板。

## v1.1.0-rc1（历史说明）

## 本次修复与验证

- 新增天天象棋 ADB、人机宿主及真人宿主布局识别，保留独立 JJ 模板与识别路径。
- 优先使用已授权宿主窗口，失败可回退 ADB 并定期探测恢复；外部显示器缓存定期刷新，窗口重开时清除旧捕获源缓存。
- 双帧确认走子；依据上一稳定布局推演漏采的两步，支持吃子及红黑翻面。
- 中盘使用稳定来源/落点标记辅助恢复回合，无可靠证据不猜先手；拒绝同方连续走子绕过重同步校验。
- 稳定实盘变化后重新对齐模拟时间线，不再永久停留在“等待盘面稳定”。
- 切换模式立即取消旧搜索；修复 Pikafish `stop` 先于 `go` 的竞态，搜索异常明确报错并退出思考状态。
- 实盘 API 不再被模拟盘或模拟回合覆盖；前端操作错误不再静默丢弃。
- 原有 14 项回归及新增 17 项专项测试通过；当前天天残局宿主实测 15/15 子、6.5～7 FPS，成功恢复红方回合并生成建议。
- **候选版边界：** 连续真实快棋、实际关闭重开窗口及双平台切换仍待完整实测；相关恢复路径已有自动化测试，不能替代生产验收。
- 打包保留两个原生启动器的既有签名与 Info.plist，不重新签名，不重置录屏权限。

## 系统要求

- Apple Silicon Mac（M1 或更新）
- macOS 14.0 或更新
- JJ 象棋默认棋子皮肤，或应用宝内天天象棋经典木纹盘面
- 首次启动需允许屏幕录制权限

发布包自带 Python 运行时、全部依赖和 Pikafish，无需安装 Python、Homebrew 或 Xcode Command Line Tools。

## 主要功能

- 自动查找重启后 Window ID 变化的 JJ / 天天象棋窗口
- 实时识别完整 32 子、残局、窗口缩放和选中特效
- 浅色网页棋盘、WebSocket 与 HTTP FEN 输出
- 本地 Pikafish 2026-09-06 + NNUE，始终执画面下方
- 红方在下与黑方在下自动适配
- 普通、进阶、高级三档棋力，选择后立即生效并持久保存
- Pikafish 崩溃透明重启，持续不可用时回退内置引擎
- 回退一步、变招分支、新局与重开同步
- 页面关闭后自动释放服务、端口和引擎进程

## 默认 AI 档位

- 普通：1 秒、2 线程、64MB Hash
- 进阶：3 秒、4 线程、128MB Hash
- 高级：8 秒、6 线程、256MB Hash

## 已知边界

- 游戏更换棋子皮肤或大幅调整棋盘美术后，需要重新采集识别模板。天天使用两种宿主布局及 ADB 固定比例网格，不支持任意裁切截图。
- 发布包版本以本文件与压缩包文件名为准；为保持 TCC 授权身份，原生启动器内部版本号维持不变。
- 打包 NNUE 禁止未经许可的商业使用，详见 `engines/pikafish/NNUE-License.md`。
- `.app` 当前为 ad-hoc 签名；首次从网络下载后若被 Gatekeeper 拦截，请在 Finder 中右键应用并选择“打开”。
- 仅用于本地学习、复盘、自建对局及平台明确允许的场景，请遵守游戏平台规则。
